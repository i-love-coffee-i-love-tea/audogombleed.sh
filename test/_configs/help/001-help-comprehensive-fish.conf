[env]
__CLI_CFG_EXEC_SILENT="y"
__CLI_CFG_LOG_LEVEL=0

[env.fish]
function create_cmd_words
    echo "thievery"
    echo "corporation"
end
function create_arg_options
    echo "opt1"
    echo "opt2"
end

[commands]
# test CLI — comprehensive help text tests
# second header line

# section alpha
alpha
	# command under alpha
	one: echo alpha-one
	# another command under alpha
	two: echo alpha-two

# section beta
beta
	# command under beta
	three: echo beta-three

# standalone command help
standalone: echo standalone

# parent with details
gamma
	## detail line one for gamma four
	## detail line two for gamma four
	# command under gamma with details
	four: echo gamma-four
	# command under gamma without details
	five: echo gamma-five

# parent with details on group
## group detail line
delta
	# command under delta
	six: echo delta-six
